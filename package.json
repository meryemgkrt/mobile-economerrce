{
  "name": "mobile-ecommerrce",
  "version": "1.0.0",
  "description": "Mobile E-commerce Full Stack App",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "install:backend": "npm install --prefix backend",
    "install:admin": "npm install --prefix admin",
    "build:admin": "npm run build --prefix admin",
    "build": "npm run install:admin && npm run build:admin",
    "start": "npm start --prefix backend"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/meryemgkrt/mobile-ecommerce.git"
  },
  "license": "ISC"
}