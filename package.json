{
  "name": "mobile-ecommerrce",
  "version": "1.0.0",
  "description": "Mobile E-commerce Full Stack App",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix admin",
    "build": "cd admin && npm install && npm run build",
    "start": "npm run start --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:admin": "npm run dev --prefix admin",
    "dev": "echo Development mode started && concurrently \"npm run dev:backend\" \"npm run dev:admin\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/meryemgkrt/mobile-ecommerce.git"
  },
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
